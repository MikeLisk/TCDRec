# Temporally Consistent Dynamic Attention for Sequential Recommendation
### Generate data
1.run the file ``data.py`` to generate the data format needed for our model. 
2.run the file ``generate_neg.py`` to generate data to speed up the test. 
### Training and Testing
Then you can run the file ``main.py`` to train and test our model.
